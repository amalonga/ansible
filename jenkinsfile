node {
    def mvnHome
    stage('Preparation') { // for display purposes
        // Get some code from a GitHub repository

        git 'https://github.com/amalonga/ansible.git'
    }
    stage('Build') {
        // Run the playbook  build
                sh ' ansible-playbook -i inventory playbook.yml'
    }
}

